export enum ApiEndpointUrl {
    LOGIN = "/auth/login",
    REGISTER = "/auth/register",
    LOGOUT = "/auth/logout",
    // Add more endpoints as needed
  }
  
  export enum ApiMethod {
    POST = "POST",
    GET = "GET",
    PUT = "PUT",
    DELETE = "DELETE",
  }
  